# Расширение JDA

JDA (Job Description Analyzer) — расширение для браузеров на Chromium, которое сопоставляет описание вакансии с вашим резюме и формирует структурированный отчёт. Все вычисления выполняются локально: текст CV не покидает устройство, API‑ключи хранятся в браузере и экспортируются только по вашей команде.

---

## ✨ Основные возможности

- **Плавающее рабочее окно** — клик по иконке открывает перетаскиваемый и изменяемый по высоте оверлей с полями ввода и результатом анализа.
- **Умный хайлайтер** — выбирайте произвольные блоки на странице, используйте undo/redo, следите за таймером выполнения.
- **Правила автоизвлечения** — стратегии CSS, DOM chain или пользовательский скрипт для сложных сайтов.
- **Гибкие промпты** — глобальные шаблоны + per-model override с токенами `{{GLOBAL_SYSTEM_PROMPT}}` и `{{RESULT_OUTPUT_TEMPLATE}}`.
- **Интеграция с Notion** — настраиваемая карта полей, экспорт одним кликом, поддержка статусов и Markdown-шаблона.
- **Импорт / экспорт** — выбор групп (провайдеры, модели, правила, шаблоны, CV, интеграции) и режимы Merge/Replace.

---

## 🔧 Установка

1. Скачайте последнюю версию на [GitHub](https://github.com/AndreyKolygin/smja-extension/releases).
2. Распакуйте архив.
3. В Chrome/Chromium:
   - Откройте `chrome://extensions/`
   - Включите **Режим разработчика**
   - Нажмите **Загрузить распакованное** и укажите папку расширения
4. Закрепите иконку «JDA» в панели — так проще открывать оверлей.

Расширение совместимо с Chrome, Edge, Brave и другими Chromium-браузерами.

---

## ⚙️ Настройки по умолчанию

После установки откройте Options. По умолчанию уже настроены:

- **Провайдеры**: Ollama Local, Google Gemini, OpenAI
- **Модели**: Llama 3 (Ollama, активна), Gemini 2.5 Flash lite (активна), GPT‑4o mini (выкл)
- **Правила автоизвлечения**: LinkedIn, hh.ru, Indeed (включены)
- **Шаблоны**: глобальный system prompt и Markdown-вывод

Рекомендуемые шаги:

1. В разделе **CV & Prompts** добавьте своё резюме.
2. Заполните API‑ключи Gemini/OpenAI, если используете облачные модели.
3. Подправьте system/output шаблоны под свой стиль.
4. (Опционально) Настройте **Integrations → Save to Notion**, чтобы экспортировать результат в базу Notion.

Сохранение происходит автоматически; при желании можно нажать Save.

---

## 🖥 Работа с оверлеем

1. Откройте страницу вакансии.
2. Кликните по иконке расширения — оверлей появится в правом верхнем углу (его можно перетащить или растянуть).
3. Нажмите **Select job description** и выделите блоки вручную, либо используйте **Auto-grab**, если для сайта есть правило.
4. Нажмите **Analyze**. В правой части появится отчёт (совпадения, требования, пробелы, рекомендации).
5. Скопируйте результат, сохраните Markdown или отправьте данные в Notion.

Описание вакансии и отчёт сохраняются, пока вы явно не очистите их, поэтому можно закрывать/открывать окно без потерь.

---

## 🔒 Разрешения

- `activeTab` / `tabs` — нужны, чтобы открывать оверлей только на активной вкладке.
- `scripting` — подсветка блоков и выполнение правил автоизвлечения.
- `downloads` — сохранение отчёта в Markdown по нажатию.
- `storage` — локальное хранение CV, настроек, API-ключей и токенов интеграций.

Дополнительные разрешения на домены запрашиваются только тогда, когда вы включаете соответствующего провайдера:

- LLM: `https://api.openai.com/*`, `https://*.openai.azure.com/*`, `https://api.anthropic.com/*`, `https://generativelanguage.googleapis.com/*`, `https://openrouter.ai/*`, `https://api.deepseek.com/*`, `https://api.perplexity.ai/*`, `https://api.x.ai/*`, `https://api.llama-api.com/*`, `https://api-inference.huggingface.co/*`.
- Локальные модели (Ollama): `http://localhost:11434/*`, `http://127.0.0.1:11434/*`.
- Интеграция с Notion: `https://api.notion.com/*`.

Пока провайдер не активирован, необходимые доменные разрешения не запрашиваются.

---

## 🔌 Интеграции и автоматизация

### Notion

- Включите **Save to Notion** (Options → Integrations), укажите integration token и database ID.
- Настройте поля: «Notion property» + «Property type» + «Source data / value». Для источников `Analysis` и `Custom text` заполните *Source data value* (префикс строки или статический текст).
- В оверлее появится кнопка **N**. Импорт/экспорт настроек включает группу Integrations и опцию сохранения токена.

### Правила автоизвлечения

Каждое правило включает:

- Шаблон сайта (домен, wildcard, regex, полный URL)
- Стратегию: **CSS**, **DOM chain** или **Custom script**
- Комментарий и флаг активности

DOM chain позволяет шаг за шагом сузить область поиска (селектор + фильтр по тексту + индекс). Скрипты работают только для iframe того же домена из-за CSP. Пользовательские скрипты выполняются в контексте страницы и имеют те же права, поэтому вставляйте только доверенный код.

---

## 📥 Импорт и экспорт

- **Export Settings** — выберите группы и при необходимости включите экспорт API‑ключей / интеграционных секретов.
- **Import Settings** — режим *Merge/add* (добавляет/обновляет по ID) или *Replace* (полностью заменяет выбранные группы).
- **Reset to defaults** — восстанавливает предустановленные провайдеры/модели/правила/шаблоны (ключи можно сохранить).

---

## 🔑 Поддерживаемые провайдеры

Google Gemini · OpenAI · Ollama · Hugging Face · Anthropic · Perplexity · OpenRouter · Azure OpenAI · Meta / xAI · DeepSeek

---

## 📄 Документация

- [Quickstart (EN/RU)](./QUICKSTART.md)
- [Changelog](./CHANGELOG.md)
- [English README](./README.md)
- [Материалы для публикации в Chrome Web Store](./docs/store/chrome-web-store.md)
- [Политика конфиденциальности](./docs/policies/privacy.md)

## 🧱 Подготовка релиза

Для сборки пакета запустите `scripts/build-release.sh` — в каталоге `dist/` появится архив `jda-extension-<version>.zip`, который можно загружать в Chrome Web Store.
