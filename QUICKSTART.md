# JDA Extension

Расширение для браузера, которое сравнивает описание вакансии с вашим резюме и выдаёт структурированный отчёт.
Все вычисления происходят локально: текст CV остаётся в браузере, API‑ключи никогда не экспортируются без вашего явного согласия.

---

## 🚀 Установка

1. Скачайте и распакуйте архив с расширением.  
2. Откройте в браузере `chrome://extensions/`.  
3. Включите **Режим разработчика**.  
4. Нажмите **Загрузить распакованное** и укажите папку с расширением.

Совместимо с Chrome, Brave, Edge и другими Chromium‑браузерами.

---

## ⚙️ Что уже настроено

При первом запуске расширение разворачивается с рабочими дефолтами:

- **Провайдеры**: Ollama Local, Google Gemini, OpenAI  
- **Модели**:
  - *Llama 3 (Ollama)* — активна по умолчанию (локально, без ключа)
  - *Gemini 2.5 Flash lite* — активна (нужен ключ Gemini)
  - *GPT‑4o mini* — отключена (включите, если есть доступ OpenAI)
- **Правила авто‑извлечения**: LinkedIn, hh.ru, Indeed
- **Шаблоны**: предустановлены system prompt и Markdown‑вывод

### Первые шаги

1. В Options → **CV & Prompts** добавьте нужные версии резюме (можно несколько).  
2. При необходимости добавьте API‑ключи в разделе Providers.  
3. Отредактируйте system/output templates под свой стиль.  
4. Если используете Notion — настройте Integrations → Save to Notion.

Все изменения сохраняются автоматически.

---

## 🐙 Локальный Ollama «из коробки»

Чтобы сразу использовать встроенные **Ollama Local** + **Llama 3**, достаточно запустить Ollama с верным списком Origins.

1. Поставьте Ollama и скачайте модель, например `ollama pull llama3`.  
2. Откройте `chrome://extensions/`, включите **Режим разработчика** и скопируйте ID расширения (вид `mbibldbjobof…`).  
3. Запустите `ollama serve`, передав ID в `OLLAMA_ORIGINS`:

   **macOS / Linux (bash/zsh)**
   ```bash
   export OLLAMA_HOST=127.0.0.1:11434
   export OLLAMA_ORIGINS=chrome-extension://<ID>
   ollama serve
   ```

   **Windows PowerShell**
   ```powershell
   setx OLLAMA_HOST "127.0.0.1:11434"
   setx OLLAMA_ORIGINS "chrome-extension://<ID>"
   ollama serve
   ```

4. В оверлее запустите анализ через Ollama и подтвердите запрос разрешения на `http://localhost:11434/*`.

Ошибка `Ollama HTTP 403 (CORS)` означает, что Ollama был запущен без `OLLAMA_ORIGINS`. Перезапустите процесс с переменной и повторите запрос. Проверить доступность сервера можно через `curl http://localhost:11434/api/tags`.

---

## 🖥 Как пользоваться

1. Откройте страницу вакансии.  
2. Нажмите иконку расширения: появится плавающее окно (overlay). Его можно перетаскивать и менять высоту.  
3. Нажмите **Select job description**, выделите нужные блоки или воспользуйтесь **Auto-grab**, если есть правило для сайта.  
4. Нажмите **Analyze** — отчёт появится в правой части окна.  
5. По необходимости сохраните результат в Notion, скопируйте Markdown или экспортируйте в файл.

Подсказка: основные действия доступны через контекстное меню (правый клик), а интерфейс можно открыть в боковой панели.

---

## 🧰 Полезные возможности

- **Block Highlighter** — подсветка произвольных блоков на странице, undo/redo, таймер выполнения.  
- **Auto-extraction Rules** — гибкие правила с CSS, групповыми DOM‑цепочками (управление активностью групп) и шаблонами, подставляющими данные страницы/schema.org.  
- **Notion Integration** — кнопка `Save to Notion` в overlay + гибкая карта полей.  
- **CV менеджер** — несколько резюме, быстрый выбор перед анализом.  
- **Sampling‑параметры** — per‑model настройки temperature/top‑p/penalty (там, где поддерживается).  
- **Импорт / экспорт** — выбор групп (включая Integrations) и опция добавлять секреты.  
- **Системные промпты** — per-model overrides с поддержкой `{{GLOBAL_SYSTEM_PROMPT}}` и `{{RESULT_OUTPUT_TEMPLATE}}`.
- **Кэш и токены** — бейдж cached и примерный счётчик токенов.

---

## 🔄 Импорт и экспорт

- `Export Settings` — выберите группы и, при необходимости, включите “Include API keys”.  
- `Import Settings` — режим **Merge** (добавляет/обновляет по ID) или **Replace** (заменяет выбранные группы).  
- `Reset to defaults` — возвращает предустановленный набор (можно сохранить ключи).

---

## 🔑 Поддерживаемые LLM‑провайдеры

Google Gemini · OpenAI · Ollama · Hugging Face · Anthropic · Perplexity · OpenRouter · Azure OpenAI · Meta / xAI · DeepSeek

---

## 📎 Ссылки

- [GitHub проекта](https://github.com/AndreyKolygin/smja-extension)
